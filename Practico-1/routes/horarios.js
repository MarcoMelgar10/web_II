const express = require('express');
const router = express.Router();
const { Horario, Cancha } = require('../models');

// This router is a placeholder; actual horario routes are handled in admin.js
// Exporting empty router to maintain structure

module.exports = router;
